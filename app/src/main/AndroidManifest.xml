<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.ufrgscaronas">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/uc_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/uc_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
        <activity android:name=".SearchRide" />
        <activity android:name=".MainProgram" />
        <activity android:name=".OfferRide" />
        <activity android:name=".OfferRide3" />
        <activity android:name=".showRides" />
        <activity android:name=".OfferRide2" />



        <activity android:name=".AboutUs" />
        <activity android:name=".MyProfile" />
        <activity android:name=".AccountSettings"></activity>
        -->
        <activity
            android:name=".Login"
            android:label="UFRGS Caronas"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".drawer"
            android:label="UFRGS Caronas"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>